# Execution (Layer 3)

This directory contains deterministic Python scripts that handle API calls, data processing, file operations, database interactions, etc.

Before creating new scripts, check for existing ones. Reliable, testable, fast. Use scripts instead of manual work. Comment well.
